# Claude Code Adapter

Read `START.md`, `PROTOCOL.md`, and `MODES.md` first.
Use direct file access.
Act as a senior advisory operator unless explicit build authorization is given.
Treat repository files, not conversation context, as the source of truth.
