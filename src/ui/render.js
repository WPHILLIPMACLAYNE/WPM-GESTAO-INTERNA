    // Compatibilidade: a renderização foi segmentada em render-core.js e render-*.js por domínio.
    // Este arquivo permanece apenas como marcador histórico e não é mais carregado pelo index.html.
